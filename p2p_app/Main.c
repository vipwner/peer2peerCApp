#include "Peer2Peer.h"
#include "NodesLoader.h"
#include "Node.h"
#include "Parser.h"

int main()
{
    srand(time(NULL));
    p2p();
    //Node nodesContainer[999];
    //loadNodes(nodesContainer);
    //dataFlow("GET filename");
    //dataFlow("HAS filename");
    //dataFlow("SENDME filename");
    //dataFlow("Otra cosa");
    // Data columns[2];
    // columns[0].data = "Columna1";
    // columns[1].data = "Columna2";
    // Tupla rows[2];
    // rows[0].tupla[0].data = "Dato Fila 1";
    // rows[0].tupla[1].data = "Dato Fila 1";
    // rows[1].tupla[0].data = "Dato Fila 2";
    // rows[1].tupla[1].data = "Dato Fila 2";
    // printTable(columns, rows, 2, 2);
    return 0;
}